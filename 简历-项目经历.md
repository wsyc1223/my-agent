# 项目经历

## 研析——基于 LangGraph 的深度研究 Agent 平台 ｜ 2026.05 - 至今

**个人独立开发** · Python 3.12 / FastAPI / LangChain·LangGraph / PostgreSQL+pgvector / Redis / DeepSeek / ragas / Langfuse / structlog / Vue3+TypeScript / Docker

**项目描述**：面向研究与知识工作场景的 AI Agent 应用，支持文件知识库 RAG 问答、深度研究自动撰写报告、工具调用人在环审批；以"分层容错 + 可验证评测"为核心方向，RAG 指标均基于自建 golden set 与 ragas 离线评测产出真实数据，Docker Compose 一键起全栈。

**Agent 编排与流式架构**：

- 基于 LangGraph StateGraph 构建可中断/可恢复 Agent 图，工具调用前人工审批、审批后 resume 续跑，PG Checkpointer 落会话级断点；深度研究采用 researcher→writer 子图，后台执行 + SSE 广播进度，显式 `recursion_limit` 并捕获 `GraphRecursionError` 降级。

**鲁棒性与三级容错（核心方向）**：

- 设计分层异常体系（`AgentError` + LLM/Tool/Infra/Business 子类），将 DeepSeek 401/402/429/500/503 及上下文超限归一为可恢复/不可恢复异常，统一 SSE/HTTP 错误帧且脱敏；三级容错：节点 `RetryPolicy` 重试 → 上下文超限 `trim_messages` 裁剪重试 → 模型 `with_fallbacks` 降级 → 重试耗尽 saga 补偿返回 `degraded` 降级报告，端到端不裸崩。
- 三态熔断器（CLOSED/OPEN/HALF_OPEN）自动快速失败与探测恢复；落库用 `asyncio.shield` 保护 + Redis 标记补偿；LLM/任务/工具三层超时参数化，工具统一错误帧 + 节点重试，`search_web`/`fetch_url` 精细分类异常并做 SSRF 防御。

**RAG 检索与离线评测**：

- 文档 SHA256 秒传 + 后台批量向量化入 pgvector，检索向量召回 + bge-reranker 精排，支持跨会话语义召回历史消息；自建 15 条 golden set，ragas 评测 **Recall@5=93.3%、Faithfulness=0.91、Answer Relevancy=0.81**，脚本可复现作回归基线。

**工程治理与可观测**：

- structlog 统一日志注入 `request_id`/`conversation_id`/`trace_id`/`node` 四字段全链路串联，全局 handler 对 500 脱敏；审计日志独立落库，slowapi 限流取真实 IP，Langfuse 全链路 trace 聚合 token 成本。
- Docker Compose 一键部署 PG+pgvector+Redis+前后端，维护 Mermaid 架构图，GitHub Actions 跑测试 + ragas 评测作回归门禁。
