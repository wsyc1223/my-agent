## 1. 这个agent的整体流程是什么？

> **💡 核心关注点：** 这是面试中最容易被问到的系统设计题，也是你展示“深度研究”架构亮点的绝佳机会。

### 为什么这么拆流程
我们采用了 **单图多阶段流水线（Single-graph multi-stage pipeline）** 模式，拆解为：`planner` -> `提纲审批(中断)` -> `researcher` -> `来源审批(中断)` -> `writer`。
- **避免幻觉与上下文爆炸**：如果让 LLM 一步到位，它极易迷失在冗长的搜索结果中。拆分阶段可以确保每一步的输出是确定的、结构化的。
- **融入人工监督 (Human-in-the-loop)**：复杂研究需要保证方向正确。通过拆分，可以在搜集资料前确认大纲，在生成报告前确认来源的可信度。

### 哪些节点是确定的workflow，哪些节点交给LLM决策
- **确定的 Workflow**：图的流转是严格的有向无环图（DAG），从计划到搜索再到撰写，包括在特定节点暂停（`interrupt_before`），这部分是代码硬编码的，保证了系统的稳定性。
- **交给 LLM 决策的**：每个节点内部的生成逻辑。例如 `planner` 决定如何将大问题拆解为多个互不重叠的子问题并生成相应的 `search_queries`；`researcher` 内部决定提取哪些摘要以及判断信息来源的可信度（high/medium/low）。

### 有没有失败分支和重试机制
- **解析重试**：如果 `planner` 输出的 JSON 格式不规范，系统会捕获 `json.JSONDecodeError`，并附带强调规则的 `SystemMessage` 发起第二次重试。
- **异常降级兜底**：在 `researcher` 抓取网页 (`fetch_url`) 或信息提取失败时，系统不会抛出异常导致整个流程中断，而是通过捕获异常记录到 `progress_log`，并降级使用原文截断的方式继续流程。

### 状态是怎么保存和恢复的
- **持久化方案**：使用了 LangGraph 的 `AsyncPostgresSaver` 结合 PostgreSQL 进行 Checkpoint 持久化。
- **恢复机制**：状态与 `thread_id` 强绑定。当触发人机交互中断时，前端发送 `resume` 请求并带上修改后的状态数据（如用户调整后的提纲或筛选后的来源下标），图谱加载 Checkpoint 后应用更新并继续向下流转。

### 多 agent 如何分工
虽然物理上是在一个 Graph 里，但逻辑上是分工明确的 Agent 集合：
- **Planner**：具有极强的逻辑拆解能力，专注于提纲生成。
- **Researcher**：偏向于执行和信息提取，负责与搜索引擎和目标网页交互。
- **Writer**：擅长长文本综合与 Markdown 排版，专注于信息的汇总与呈现。

### 如何避免多个 agent 相互扯皮或者是死循环
- **架构级约束**：采用的是**流水线架构（Pipeline）**而不是全连接的网络图。节点之间的流转方向是单向的（没有向后的反馈环），从根本上消除了因为大模型互相推诿导致的死循环风险。

---

## 2. 如何设计 tool calling

> **💡 核心关注点：** 展现你的工程健壮性设计。大模型经常会胡乱调用工具，如何兜底是高级工程师的体现。

### tool 的输入输出schema 如何定义
使用 Pydantic BaseModel 或者具有清晰类型注解和 Docstring 的 Python 函数，让 LangChain 自动转换为 OpenAI 兼容的 JSON Schema。

### 工具调用失败怎么处理 / 工具参数错误怎么兜底
- **绝不让异常崩溃服务**：所有的工具函数内部都要加上全局 `try-catch`。如果发生网络超时或参数解析错误，返回形如 `"错误：xxxx，请尝试更换关键词"` 的字符串给大模型，让大模型知道失败了并自主进行下一步决策（例如重试其他搜索词），而不是让后端抛出 500 错误。

### 如何防止模型调用危险工具（🔥 面试加分项）
- **SSRF 防护（Server-Side Request Forgery）**：在 `fetch_url` 等可以让模型发起网络请求的工具中，必须前置增加 URL 安全校验模块（如 `is_safe_url` 函数）。通过解析域名并在 DNS 层获取真实 IP，拦截所有私有 IP 段（如 `127.0.0.1`，`192.168.x.x`）和非 HTTP 协议（如 `file://`），防止模型被恶意 Prompt 操控扫描内网服务或读取本地敏感文件。

### 如何判断是 promopt 能力还是模型能力问题
- 如果模型反复违反格式要求（比如让输出 JSON 偏要带一堆解释废话），通常是 **Instruction Following（指令遵循）** 能力不足（属于模型能力边界）。
- 如果模型能遵循格式，但提取的信息不准或者没有侧重点，多半是 **Prompt 描述不够具体**，可以通过给出 Few-shot 示例来改善。

---

## 3. Context Window (上下文窗口) 与记忆机制

### 为什么要分层记忆
在长周期任务（如深度研究）中，如果把所有读过的几十页网页源码全都扔进传统的 `messages` 列表里，极易触发 Context 爆炸，导致模型不仅响应极慢，而且容易遗忘前面的核心指令（Lost in the Middle 现象）。

### 项目级规则、用户偏好、会话上下文应该怎么隔离
- **项目级规则/偏好**：应该被固化在 `SystemMessage` 中。
- **会话上下文**：传统的闲聊历史存放在短期的 `messages` 列表里。
- **深层研究状态**：采用结构化的 `State`（字典对象，包括 `outline`, `sources`, `report`），每进入下一个阶段，只传递提炼过的高密度信息（比如摘要），丢弃原始庞大的网页源码，以此保护 Context Window。

---

## 4. 我的 RAG 需要做哪些优化

### Recall@K 和 Precision@K 是什么，有什么区别，怎么取舍
- **Recall (召回率/找全率)**：数据库里相关的文档，有多少被你捞出来了。
- **Precision (准确率)**：你捞出来的这些文档里，有多少是真的相关的。
- **取舍**：在 LLM 应用中，由于 Context Window 的限制以及模型容易被无关信息干扰产生幻觉，通常我们更看重 **Precision@K**（即使漏掉一些，但给到 LLM 的必须是高质量相关的）。

### 如果 BM25 已经很好了、向量检索还有必要吗
有必要。BM25 是词法检索，严重依赖关键字匹配；而向量检索基于语义匹配（例如用户搜“苹果手机”，文档里只有“iPhone”，向量能懂，BM25 抓瞎）。业界目前最佳实践是 **混合检索（Hybrid Search: BM25 + 向量）** 然后做 RRF (Reciprocal Rank Fusion) 重排。

### RAG 的一个大坑：当前会话污染（🔥 面试加分项）
如果不做隔离，用户刚刚发出的 Query 和系统刚回的 Answer 也会被向量化存入库中。下一次搜索时，最相似的结果往往就是刚才说的废话。所以一定要在 SQL 层面上增加 `exclude_conversation_id` 的设计，剔除当前正在对话的内容。

---

## 5. Agentic RAG 和传统的 RAG 有什么区别

### Query Rewrite 是否算 Agentic
算。这打破了传统 RAG “用户给啥就搜啥”的僵化模式。让 LLM 根据上下文重写、扩写（HyDE）或拆解 Query，是赋予检索系统“思考”能力的第一步。

### 检索失败后 Agent 该如何调整策略呢
传统的 RAG 搜不到就直接回答“资料库中没有”。Agentic RAG 会根据搜索为空的结果，自主决策缩减搜索条件、更换近义词或者发起另外一个侧面的查询，进行多轮迭代。

### Agentic RAG 的成本和稳定性问题如何解决
不可控的连续搜索会导致极其昂贵的 Token 消耗。这就体现了我们的 **深度研究流程引入提纲和人工审批** 的价值。通过显式的人机交互断点（Interrupt），把失控的风险关在笼子里。

---

## 6. Agent 框架问题

### langgraph 和 langchain 的区别是什么
- **LangChain**：是一个组件化的工具箱（大百科），提供统一的模型接口、工具链、Prompt 管理等。
- **LangGraph**：是建立在 LangChain 之上的**状态机编排框架**。它的核心职责是处理 Agent 的循环行为、多阶段工作流、以及流转状态的持久化（Checkpointing）。

### langgraph 里面的 state、node、edge 分别是解决什么问题
- **State**：全局黑板。定义了在图流转过程中，大家共享和更新的数据结构（如我们的 `ResearchState`）。
- **Node**：执行单元。通常是一个 Python 函数或者一个被包装的 LLM，负责读取 State，执行任务，然后返回需要更新的 State 增量。
- **Edge**：路由规则。定义了跑完 Node A 之后，接下来该去 Node B，还是根据条件去 Node C。

### 为什么选 langgraph 而不是 AutoGen、CrewAI 或者是手写状态机
因为我们的项目强依赖于 **Human-in-the-loop（人机交互中断）** 和 **流式状态恢复**。LangGraph 底层原生的 `Checkpointer` 机制完美契合了我们的需求（存入 PostgreSQL 并在未来任意时刻通过 `thread_id` 恢复）。相比之下，CrewAI 过于黑盒难以做细粒度控制，而手写状态机在处理异步持久化和断点恢复时会产生极大的工程负担。

---

## 🌟 结合最新实施计划的工程亮点补充（强烈建议写入简历/面试讲述）

作为正在做这个项目的同学，你不仅要把功能做出来，还要展现出你在实现过程中解决的“硬核工程问题”。以下是基于我们最新的架构和计划，为你梳理的 4 个加分项：

1. **解决大模型并发下的异步阻塞问题 (Async I/O in FastAPI)**
   - **痛点**：在早期的代码中，大模型的 Embedding 提取 (`SentenceTransformer`) 属于纯 CPU 密集的同步操作，直接放在 FastAPI 的协程函数里会阻塞整个 Event Loop，导致并发量只要超过 1，整个服务就会卡死。
   - **你的解决方案**：主动识别出这一性能瓶颈，使用 `await asyncio.to_thread(embed_text, message)` 将同步阻塞调用卸载到线程池中执行，从而释放主事件循环，保证了异步后端的高并发吞吐能力。

2. **多工具并发调用的协议对齐 Bug (Multi-tool call alignment)**
   - **痛点**：当大模型同时触发多个并行的工具调用时，如果用户在审批节点选择了拒绝，老系统只返回了一条拒绝消息给大模型，导致 `tool_calls` 的 ID 数量与历史消息中 `ToolMessage` 的数量无法对齐，直接引发 OpenAI/DeepSeek 接口报错崩溃。
   - **你的解决方案**：修复底层恢复逻辑，通过解析 `last_msg.tool_calls` 列表，为每一个工具调用构造独立的拒绝 `ToolMessage`，实现了严格的协议对齐，体现了对底层协议规范的深刻理解。

3. **构建企业级的服务器端防请求伪造屏障 (SSRF Protection)**
   - **痛点**：允许 Agent 自由抓取网页(`fetch_url`)，相当于在内网开了一个后门。攻击者可以让大模型去请求 `http://169.254.169.254` (云服务器元数据) 或 `http://127.0.0.1:6379` (内网 Redis) 等。
   - **你的解决方案**：不是简单的正则匹配，而是引入了 DNS 解析和 IP 校验。在请求前获取目标真实 IP，使用 `ipaddress` 库严格拦截 `.is_private` 和 `.is_loopback` 地址，保证了系统的系统安全性。

4. **规范化的服务器推送事件流 (SSE Protocol Design)**
   - **痛点**：Agentic Workflow 的执行过程可能长达几分钟，前端如果不做细粒度进度反馈，用户会以为死机了。
   - **你的解决方案**：没有使用粗糙的长轮询，而是设计了一套结构化的 SSE (Server-Sent Events) JSON 协议（定义了 `progress`, `outline_ready`, `report_chunk` 等不同类型的信令）。并在前端使用 Vue 3 结合 Pinia，实现了状态的流式拼接和响应式更新，提供了极佳的打字机特效和时间线进度感知。
