#前端设计方案

## 聊天主界面设计

### 聊天主界面
用户登录完成之后进入主界面，这个主界面是一个普通聊天的界面，主界面是一个简单的的对话框居中展示，对话框下方有一个深度研究的选项，当用户点击了会进入深度研究界面。

### 左侧边栏设计
左侧是一个聊天的历史会话列表以及个人信息，其中个人信息的左侧边栏的最下方,内容包括用户的头像和用户名即可，然后有一个齿轮图标，用户点击了齿轮图标能够进入用户信息主界面。这个侧边栏可以通过一个箭头按钮实现展示和隐藏。

### 右侧边栏设计

修改一下原本的Agent状态显示（呼吸灯条来显示思考中、调用工具中等等状态），把这些统一展示在右侧边栏，需要简约展示模型的思考，工具调用，中断重连的确认等待等等。同时也有一个箭头按钮能够实现一键隐藏。

### 刻度栏实现对话跳转
在主界面的最右侧，有一个刻度栏，其中每一个刻度栏代表的是用户的当前会话里面的一条消息记录，这个消息记录包括用户和Agent的一问一答算作一条。当有侧边栏隐藏时这个就默认放在最右侧，当右侧的侧边栏显示的时候这个放在侧边栏的左侧边缘。刻度栏默认呈现灰色状态，并且较小可以参考deepseek的设计，不能够太显眼抢了镜头。当用户鼠标悬浮在刻度栏上方对应的刻度会加粗橙色高亮并显示对应的消息的用户提出的问题，作为一个浮动的字样呈现在旁边，鼠标离开时刻度回归默认状态，用户点击的时候屏幕跳转到该对话，将用户的本次发出的消息默认滑动到屏幕的最顶部。

## 深度研究界面设计

### 深度研究主界面
这个界面是当用户在聊天界面点击了深度研究的时候会跳转的默认界面，与聊天完全分隔，不会共享历史消息等等，这个界面需要采用卡片化布局，首先默认当前界面只有两个卡片，一个是居中的约占用屏幕三分之一的聊天卡片，上下均保留二个像素左右，卡片间隔大于二个像素左右，另一个是一个占屏幕六分之一的消息历史卡片，贴聊天卡片左侧，间隔二个像素，上下均保留二个像素左右，和聊天卡片保持对齐。然后是一个文件展示卡片，这个卡片默认是需要隐藏的，占据屏幕的二分之一，当这个卡片出来的时候原本的聊天卡片和消息历史卡片需要向左侧移动直接距离屏幕的左侧保留二个像素，但是大小和高度不可以改变，只是水平横向移动而已，由于文件展示卡片占据二分之一，聊天卡片占据三分之一，消息历史卡片占据六分之一，所以此时文件展示卡片出来的时候应该三个卡片占据了所有的屏幕，但是需要保留两个像素的间距。

### 中部对话框要求
#### 消息发送前
对话框中的需要有默认的占位文字，类似于我是你的深度研究助手可以帮你写报告之类的。对话框在卡片中上下左右均居中，对话框下方有一个上传文件的按钮，用户可以通过这个上传文件。文件上传后会在对话框中显示，不同的文件需要显示不同的小卡片，卡片约是三行文字高度，显示在对话框里面，需要扩展对话框高度来显示，其中文件在上，文字在下，中间有半个文字高度的行距，如果用户上传了文件同时输入了文字，需要在输入框的上半部分扩高输入框展示文件卡片，原本的部分依旧展示文字。文件的图案上面需要有文件的名字以及类型，如果名字太长只需要展示前几个字即可，但是一定需要展示文件类型，文件名字过长的部分可以使用省略号代替，当用户鼠标悬浮在文件上面时需要悬浮展示文件全名。

#### 消息发送后
当用户的消息发送出去的时候，如果说用户的消息里面没有任何文件，则类似于普通的对话模式一样展示文字即可。如果用户的消息中有存在文件，需要在用户的文字气泡下方展示文件卡片，卡片需要显示文件名字和类型，要求和之前提到的一样。此时当用户点击了文件的时候，会在屏幕的右侧展示出一个占屏幕宽二分之一的卡片，上下均保留两个像素，和对话框的卡片保持对齐。这个卡片主要用于展示文件，要求见下方。

### 文件展示卡片要求
文件展示卡片的顶部需要留出一部分区域用来展示文件名，大概一个文字的行高左右，当需要查看自己发送的文件或者是查看ai返回的文件都会在这展示，所以有可能需要展示多个文件，如果多个文件的话顶部需要有一个文件名的列表，当用户点击了文件名则会展示对应的文件，类似于代码编辑器里面的每一个代码文件的标签页。下面全部都是内容展示部分，用户可以拖动滚动条或者是使用鼠标滚轮，同时需要支持全局搜索，在标签页的下方需要有一个放大镜的搜索图标，当用户点击的时候会展示出来一个搜索框，搜索框的最右侧的有一个叉号可以用来关闭。其中有一个占位文字是“搜索文件内容”，当用户输入了文字之后无需点击回车或者是发送，文件会实时对于用户的当前输入文件进行检索匹配，然后对于所有匹配内容会进行同步蓝色高亮展示直至用户按下回车，用户输入结束后需要按下回车，文件会自动跳转到距离用户当前的浏览位置最近的可匹配选项，并且所有匹配选项都会进入橙色高亮状态直至用户关闭搜索框。

### 历史消息展示要求
历史消息是一个占据屏幕宽度六分之一的卡片，其中包括了用户的历史聊天记录，以及在最下方同样需要有一个用户的简要展示包括头像和昵称，以及一个锯齿形状的按钮可以跳转到用户的信息界面，这个所跳转到的用户信息界面和聊天模块里面齿轮按钮的跳转到的是同一个界面，都是用户的个人信息界面。

## 用户的个人信息界面
这个界面需要包含用户的个人信息。
