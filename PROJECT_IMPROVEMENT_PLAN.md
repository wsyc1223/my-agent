# Agentic OS 项目落地改进规划

## 目标

把当前项目从“本机能演示的 Agent 原型”推进到“可以稳定启动、可测试、可部署、能作为实习作品展示”的完整项目。

当前项目已经具备有价值的基础能力：FastAPI 后端、Vue 前端、JWT 登录、多会话、SSE 流式输出、LangGraph 工具审批、PostgreSQL checkpoint、pgvector 历史记忆和 Langfuse 可观测性。下一阶段不要继续盲目加功能，先补齐工程闭环。

## 当前主要问题

1. 后端主应用当前不能正常导入，`backend/src/router/agent.py` 引用了不存在的 `ChatResponse`。
2. Git 仓库状态异常，当前目录不能被 `git status` 识别为正常仓库。
3. 前端 API 地址之前分散在多个文件里，已经开始出现配置不一致。
4. 后端错误处理和 CORS 仍是开发期写法，不适合部署展示。
5. RAG 检索没有排除当前会话，可能造成上下文重复。
6. `backend/src/models` 本地模型约 1.5G，仓库体积管理需要明确方案。
7. 自动化测试不足，`test-project.sh` 对后端导入失败的错误信息暴露不充分。
8. 项目说明、架构图、部署说明和演示脚本还不足以支撑面试展示。

## 阶段一：先让项目稳定启动

**目标：** 后端、前端、数据库相关基础链路可以被明确验证。

任务：

- 修复 `ChatResponse` 导入错误，确保 `python -c "from main import app; print(app)"` 成功。
- 修改 `test-project.sh`，不要吞掉主应用导入的 stderr。
- 梳理 `.env.example`，列出必要变量：大模型、数据库、JWT、Langfuse、Tavily、前端 API 地址。
- 确认 `start-dev.sh` 能在干净终端中启动前后端。
- 明确 PostgreSQL、Redis、pgvector 的本机启动方式，优先用 `docker-compose.yml` 固化。

验收标准：

- `./test-project.sh` 成功且失败时能显示真实错误。
- 后端 `/docs` 可访问。
- 前端登录页、聊天页可以正常访问。
- README 中有 5 分钟以内的本机启动步骤。

## 阶段二：建立最小测试体系

**目标：** 项目不是“手动点过能跑”，而是关键行为有自动化验证。

后端测试优先级：

- 应用导入测试：确保 `main:app` 可导入。
- Auth 测试：注册、登录、错误密码、重复邮箱。
- 会话权限测试：用户只能访问自己的 conversation。
- SSE 事件测试：`conversation_id`、`text`、`interrupt`、`done` 格式稳定。
- RAG 检索测试：排除当前会话，避免重复上下文污染。

前端测试优先级：

- 构建测试：`npm run build` 必须通过。
- API 配置测试：所有请求统一走 `src/services/api.ts`。
- 登录流程测试：登录成功后 token、userId、userName 状态一致。
- 流式事件解析测试：标准 SSE JSON 事件可以正确更新消息。

验收标准：

- 后端至少 10 个 pytest 用例。
- 前端至少保留构建检查和关键状态逻辑测试。
- 每次修改后有固定验证命令。

## 阶段三：收紧安全边界

**目标：** 从开发期安全策略升级到可展示、可部署的安全策略。

任务：

- CORS 从 `allow_origins=["*"]` 改成环境变量白名单。
- 生产环境错误响应不返回 `str(exc)`，详细 traceback 只写日志。
- 注册、登录增加基础输入校验：邮箱格式、密码长度、昵称长度。
- 工具调用审批从“所有工具都拦截”升级为“高风险工具拦截，低风险工具自动执行”。
- 明确 JWT 存储策略：本地演示可以继续使用 `localStorage`，部署展示建议改为 httpOnly Cookie。
- 对 `fetch_url` 增加 URL 协议和内网地址限制，避免 SSRF 风险。

验收标准：

- `.env` 可以控制允许的前端来源。
- 后端 500 响应不泄露内部异常细节。
- 工具审批有清晰的风险分级说明。

## 阶段四：优化 Agent 与 RAG 质量

**目标：** 让 Agent 表现更稳定，而不是只靠 UI 包装。

任务：

- `search_messages` 增加 `exclude_conversation_id`，跨会话记忆不要重复当前上下文。
- 给检索结果加长度限制和去重策略，避免把过长历史直接塞入 prompt。
- 增加系统提示词，明确 Agent 的能力边界、工具使用规则和中文回答风格。
- 为工具结果设计结构化返回格式，前端可以展示工具名、参数、结果和审批状态。
- 对空内容 AIMessage、ToolMessage、异常中断状态增加防御性处理。
- 记录每轮 Agent 的输入摘要、工具调用、最终回答，方便 Langfuse 回放分析。

验收标准：

- RAG 命中的历史不会包含当前用户刚发出的消息。
- 工具调用失败时前端能显示清晰错误，而不是空白或无限 loading。
- 至少准备 5 个稳定演示问题，覆盖普通问答、联网搜索、网页读取、计算、拒绝工具调用。

## 阶段五：整理项目结构和依赖

**目标：** 让项目看起来像一个认真维护的工程，而不是本机堆出来的目录。

任务：

- 修复或重新初始化 Git 仓库，确保 `git status` 正常。
- 保留 `backend/uv.lock` 和 `backend/.python-version`，保证环境可复现。
- 删除或归档废弃入口，例如根级 `frontend/index.html`。
- 清理不该进入仓库的目录：`.venv`、`node_modules`、`dist`、`__pycache__`。
- 明确本地模型管理方案：下载脚本、外部路径、对象存储或 Git LFS，不能直接把 1.5G 模型当普通源码管理。
- 把前端真实工程目录从 `frontend/my-vue-project` 重命名为更专业的名称，例如 `frontend/app`。

验收标准：

- 新同学 clone 项目后能按 README 复现环境。
- 根目录结构一眼能看懂：backend、frontend、docs、scripts、README。
- 大文件、密钥、构建产物不会混进版本管理。

## 阶段六：补齐作品展示材料

**目标：** 面试官不用读完整代码，也能理解你做了什么、难点在哪里、你解决了什么问题。

任务：

- 写一份高质量 README：项目定位、核心功能、技术架构、启动方式、演示账号、截图。
- 增加架构图：前端、FastAPI、LangGraph、PostgreSQL、pgvector、Langfuse、外部工具 API。
- 增加“技术难点”章节：SSE 流式输出、人机审批、checkpoint、RAG 记忆、工具安全。
- 增加“我独立完成的部分”和“下一步规划”，方便实习面试表达。
- 录制 2-3 分钟演示视频或准备 GIF。
- 准备 5 个演示脚本，避免现场随便问导致工具或外部 API 不稳定。

验收标准：

- README 可以让陌生人 10 分钟内理解项目价值。
- 简历项目描述能对应到代码、截图、演示和测试。
- 面试时可以清楚回答：为什么用 LangGraph、怎么做审批、怎么避免越权、怎么做历史记忆。

## 推荐执行顺序

1. 修后端启动错误。
2. 修 Git 仓库和 `.gitignore`。
3. 补 `.env.example` 与 README 启动步骤。
4. 加后端最小测试。
5. 修 CORS、异常响应、RAG 当前会话污染。
6. 做工具审批分级。
7. 整理前端状态和 API 模块。
8. 补展示材料和演示脚本。

## 近期两周建议

第一周只做工程闭环：

- 第 1 天：修后端导入错误和测试脚本。
- 第 2 天：整理 Git、`.gitignore`、`.env.example`。
- 第 3 天：补 README 启动说明。
- 第 4-5 天：补 Auth、Conversation、应用导入测试。
- 第 6-7 天：修 CORS、异常响应、API 配置和 RAG 当前会话排除。

第二周做展示质量：

- 第 1-2 天：工具审批分级。
- 第 3 天：前端展示工具调用详情。
- 第 4 天：准备 5 个稳定演示用例。
- 第 5 天：补架构图和 README 技术难点。
- 第 6-7 天：录演示视频，整理简历项目描述。

## 判断标准

这个项目真正落地，不是指功能越多越好，而是满足以下条件：

- 能一键或按文档稳定启动。
- 核心功能有自动化测试。
- 安全边界说得清、代码里也做得到。
- 出错时可观测、可定位、可恢复。
- 面试官能从 README、演示、测试和代码中看到你的独立工程能力。
